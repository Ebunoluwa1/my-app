import styled from "styled-components";

export const StyledDashboard = styled.div`
           font-size: 18;
           margin: 0 auto;
            padding: 10px;
             .link{
                cursor: pointer;
                text-decoration: none;
             }
`